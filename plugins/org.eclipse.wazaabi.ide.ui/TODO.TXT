propertysheets
	rajouter la gestion du CTRL+Z/CTRL+Y  dans le propertysheet
	
create a document from scratch
gérer les layoutEditpart, layoutDataEditparts (ajout/suppr/move etc...)


Dans Sabri.ui, on ajoute un bouton, il apparait uniquement si on bouge le viewer
==> tester le cas directement avec Wazaabi


première selection du container n'affiche pas la selection dans le viewer (eclipse ???)


Next step :

* créer modif d'un style boolean
* editpart des layout data
* property sheet des layout data (grid data)



* editpart du binding
* filtrage du binding (comme layout/layout data)


Property Sheets :

gestion du ctrl+Z/ctrl+Y
doubleclick sur item du tree => ouverture du property sheet


Method & Code descriptors :

Depuis le method descriptor on doit pouvoir invoker sans utiliser le code.descriptor (a faire en semptembre)
------------------------------------------------------------------------------

En fait, dans ton code du BindingUtils.bindUiToDomain(), tu l'a mis sauf que tu l'as ecrase apres avec core:ui:default:selection.
Bref ! Ca marche. Et j'ai update pour tester le dnd.

Toutefois, je vois deux bugs :
 - si tu dnd deux elements (Label + Text), le second (Text) atterit en premier dans l'arbre lors du drop, ce qui donne (Text + Label)
 - tes modifs dans le modele ne marque pas l'editeur de modele comme dirty. Pourtant tu utilises bien des commandes ?

A+
Sylvain

------------------------------------------------------------------------------